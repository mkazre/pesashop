{
  "name": "ecommerce-platform",
  "version": "1.0.0",
  "description": "Advanced E-commerce Platform with React, Node.js, MongoDB and Flutter API",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend:dev\" \"npm run admin:dev\"",
    "backend:dev": "cd backend && npm run dev",
    "admin:dev": "cd admin-panel && npm run dev",
    "backend:start": "cd backend && npm start",
    "admin:start": "cd admin-panel && npm run build && serve -s build",
    "install:all": "npm install && cd backend && npm install && cd ../admin-panel && npm install"
  },
  "keywords": [
    "ecommerce",
    "react",
    "nodejs",
    "mongodb",
    "flutter-api"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
