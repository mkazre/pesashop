# Copy this to .env and set your API URL
# For local development, use your machine's IP (not localhost) so the phone can reach it
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.100:5000
EXPO_PUBLIC_API_URL=http://localhost:5000
