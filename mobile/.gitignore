node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.local
ios/
android/
builds/

# Credentials — never commit these
google-play-service-account.json
GoogleService-Info.plist

# google-services.json contains only the Firebase Android *client* config
# (project id + public API key) needed for FCM push — not a secret per
# Google's own docs, and EAS Build needs it present in the uploaded
# project (gitignored files are excluded from the build archive).
!google-services.json

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli